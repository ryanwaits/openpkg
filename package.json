{
  "name": "openpkg",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run build:spec && bun run build:sdk && bun run build:cli && bun run build:fumadocs-adapter",
    "build:spec": "cd packages/spec && bun run build",
    "build:sdk": "cd packages/sdk && bun run build",
    "build:cli": "cd packages/cli && bun run build",
    "build:fumadocs-adapter": "cd packages/fumadocs-adapter && bun run build",
    "build:site": "cd apps/site && bun run build",
    "dev": "bun run --parallel dev:spec dev:sdk dev:cli",
    "dev:spec": "cd packages/spec && bun run dev",
    "dev:sdk": "cd packages/sdk && bun run dev",
    "dev:cli": "cd packages/cli && bun run dev",
    "dev:site": "cd apps/site && bun run dev",
    "lint": "biome check packages/ apps/",
    "lint:fix": "biome check --write packages/ apps/",
    "format": "biome format --write packages/ apps/",
    "changeset": "changeset",
    "version": "changeset version && bun install",
    "release": "NPM_TOKEN=$NPM_DOCCOV_TOKEN changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@changesets/cli": "^2.29.7",
    "@types/bun": "latest",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "vercel": "^48.10.12"
  }
}
